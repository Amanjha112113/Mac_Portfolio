import { Desktop } from '@/components/Desktop';

/**
 * Root page — renders the macOS-inspired desktop environment.
 * The desktop shell must render independently of any API calls.
 */
export default function Home() {
  return <Desktop />;
}
